name: Update

on:
  schedule:
    - cron: '20 8,1 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout current repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
    
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r crawler/requirements.txt

      - name: Create required directories
        run: |
          mkdir -p ${{ github.workspace }}/crawler
          echo "Directory created"

      - name: Checkout target repository (crawler files only)
        uses: actions/checkout@v4
        with:
          repository: wxbtt/wz
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}
          path: target-repo
          sparse-checkout: |
            crawler/config.py
            crawler/main.py

      - name: Copy required files from target repository
        run: |
          # Copy config.py and main.py from target repo's crawler folder
          if [ -f "target-repo/crawler/config.py" ]; then
            cp target-repo/crawler/config.py $GITHUB_WORKSPACE/crawler/
            echo "Copied config.py to crawler folder"
          else
            echo "Error: target-repo/crawler/config.py not found"
            exit 1
          fi
          
          if [ -f "target-repo/crawler/main.py" ]; then
            cp target-repo/crawler/main.py $GITHUB_WORKSPACE/crawler/
            echo "Copied main.py to crawler folder"
          else
            echo "Error: target-repo/crawler/main.py not found"
            exit 1
          fi

      - name: Run main.py
        run: |
          cd ${{ github.workspace }}/crawler
          python main.py
          ls -l  # Check the generated files

      - name: Update current repository with results
        run: |
          # Add any files generated by main.py to current repository
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "Update with latest crawl results"
          git push

      - name: Clean up
        run: |
          # Remove the copied Python files
          rm -rf ${{ github.workspace }}/crawler/config.py
          rm -rf ${{ github.workspace }}/crawler/main.py
          echo "Clean up completed"